# blocknet-pow-kernel

Reusable fixed-parameter Argon2id kernel extracted from Seine's CPU backend
for reuse by external verifiers.

This crate exports:
- `FixedArgon2id`
- `PowBlock`
- `ReusablePowContext`
